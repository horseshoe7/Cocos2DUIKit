platform :ios, "6.0"
pod "cocos2d", "~> 2.1"

# how you can have dependencies just for the test target.  by declaring exclusive: false, means it also includes the above
# we declare exclusive => true because the above pods are linked against the MPax target, and the Unit testing target itself
# is linked against that.

#target :test, :exclusive => true do
#    link_with 'MyTestTarget-Name' #i.e. the test target name in your project
#
#    pod 'OCMock', '~> 2.1.1'
#    pod 'Expecta', '~> 0.2.1'
#
#end
